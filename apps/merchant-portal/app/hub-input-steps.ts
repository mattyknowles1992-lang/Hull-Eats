/** Number input spinner step for money fields across the merchant hub (£). */
export const HUB_PRICE_INPUT_STEP = 0.1;

/** Number input spinner step for miles / delivery distance fields. */
export const HUB_DISTANCE_MILE_INPUT_STEP = 0.01;
